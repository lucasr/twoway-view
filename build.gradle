buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:0.7.+'
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = "1.10"
}
